# <Feature>

<Một câu: feature này làm gì.>

Entry point: <FeatureController hoặc file controller chính>
Public API: `api/` (`<Feature>Module`)
Storage: <FeatureRepository / tên bảng chính>
Depends on: <FeatureA/api>, <FeatureB/api>

<Nếu có: constraint/decision đặc biệt, 1–3 dòng, hoặc "xem DECISIONS.md #N".>
